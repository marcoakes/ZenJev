# Execution decision ready for owner selection

This document records proposed scope, not approval. The current Apple Container route cannot be inspected or used from this coding session. The connected reports profile is unrelated. No verified ZenJev runtime digest is available, so no ready-to-run contained profile has been invented.

## Proposed direct build with standalone Wringer verification

Build the complete product specified by `ASTRA_MASTER_BUILD.md` in this separate local repository using the current Codex coding session and its subagents. Run actual product checks through Wringer's supported, explicitly labelled `trusted_local` standalone verifier. No contained ACP journey, separate Claude/Codex API workers, paid provider smoke or live Jev evaluation would be claimed or launched.

Bound this development attempt to three hours of active implementation/verification, with up to three concurrent coding-session subagents, zero separate provider API worker sessions, zero provider connectivity requests, and at most three repair cycles for any unchanged failing acceptance group. Preserve work, failures and unresolved outcomes across interruption. These limits are not a monetary cap on coding-app usage. Stop for changed protected criteria, an unavailable prerequisite, exhausted limits or a genuine owner decision.

Scope: all six required application routes; Node/Next/React/TypeScript; PostgreSQL/Prisma; durable worker; 100 tickets/20 issues/12 organisations; deterministic mock decisions; review/override/associations; exact approvals/dry runs/audit; Jev, GitHub and Zendesk HTTP adapters tested with fixtures; authentication/privacy/reconciliation; evaluation and saved-prediction threshold simulator; approved visual identity; startup, tests and handoff documentation.

Dependency and tooling changes stay inside this repository and task workspace. Use an available local PostgreSQL installation, or inspect and pin project-local PostgreSQL binaries; do not install system tools silently. Keep Docker Compose as the documented supported developer alternative. Ordinary offline demo/tests run without external credentials. Public documentation and package downloads are permitted setup traffic, not app integration traffic.

Verification: retain honest baseline failures, freeze meaningful security/acceptance assertions, then run lint, typecheck, unit/HTTP contract tests, real PostgreSQL integration/restart/concurrency tests, Playwright workflow/accessibility/responsive tests and production build. Wringer records exact source identities, commands, exits and output. Capture actual app screenshots after functionality passes. Tests and agent review do not supply the owner's human usability verdict.

Use the parser-validated proposal in `standalone-proposed.yaml` as the verification configuration after route selection; its listed tests are targets to implement, not existing passes. Do not claim command failures caused by missing prerequisites as meaningful behavioral baseline failures. Requirement coverage must map every SW/ZJ/Part II acceptance requirement, including those not proved by the grouped gates.

No GitHub repository creation, push, PR, visibility change, deployment, live customer-data processing or external ticket/issue mutation is included.

## Retain the contained route instead

The owner/operator enables Apple Container access, inspects or provisions a digest-pinned Node/PostgreSQL/Playwright/ACP image and measures its boundary. Prepare the ZenJev source via Wringer's supported `prepare --local` route into a new external controller. Compile the exact product profile and obtain a real bounded work approval before provider workers run. Existing reports jobs and their reservations remain untouched. Do not copy a historical image digest and describe it as newly measured.

## Why a selection is required

`ASTRA_MASTER_BUILD.md` §5 says: “If the contained route is blocked, preserve the exact evidence and continue only independent authorised preflight/documentation work. Present a distinct direct-build/standalone-verification route for owner selection rather than silently taking it.” Part I §4 repeats that a direct build must be newly authorised. This is the requested handoff's explicit route boundary, not a new approval rule invented by the assistant.
