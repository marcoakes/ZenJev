# Execution route and delegated review

The contained route was blocked by Apple Container `Operation not permitted`. A concrete direct-build/standalone-verification route was presented in this conversation. The subsequent instruction to act as the reviewer and fill everything out authorised proceeding with ordinary engineering decisions on that route. The scope/limits and actual choices are recorded in [decisions](decisions.md).

Actual route: current Codex coding session plus its three subagents, separate local ZenJev repository, unchanged Wringer alpha.14 standalone `trusted_local` verification. No API build worker, contained ACP run, new controller approval, paid provider call or publication was performed. Existing reports jobs/approvals were neither copied nor changed.

Reviewer identity is **Codex, delegated agent**. No owner name, signature, authenticated human-presence event or personal usability verdict has been filled in. The agent review and actual test results appear in [test report](test-report.md). That distinction does not block the authorised local build and review work.

The earlier `standalone-proposed.yaml` is retained as a historical proposal; `.wringer.yaml` is the runnable verification configuration. Read [requirement evidence](requirement-evidence.md) for assertions and limits rather than interpreting a gate's `proves` identifiers as proof of every associated requirement.
