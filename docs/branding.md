# ZenJev branding

Canonical tracked image: `public/branding/zenjev-hero.png`.
PNG dimensions: 1254 × 1254. SHA-256:
`12d814fe08a70d57764a0c8d27ad6b0bb43f9b7706407b79a6d05217c1a54d7d`.

The attachment has been preserved byte for byte. The root README uses a relative link. The Next.js app serves `/branding/zenjev-hero.png` locally with the complete supplied alt text, an undistorted square aspect ratio and a failure fallback. Use it in a compact welcome/about surface while keeping the ticket queue primary. No crop, recolouring, redraw, new labels, remote image dependency or generated replacement is authorized.

Tokens specified by the brief:

```css
--zenjev-pink: #FC84A2;
--zenjev-ink: #111111;
--zenjev-paper: #F1EBE3;
--zenjev-panel: #FFFFFF;
--zenjev-grey: #DEDEDE;
--zenjev-deep-pink: #A8174A;
```

Use readable UI typography, sparse monospace accents, visible keyboard focus, labelled controls and textual status. Keep halftone decoration away from working text. Actual contrast, responsive and browser results are recorded in test-report.md; automated accessibility coverage does not substitute for every assistive-technology workflow.
